import ErrorMsg from "@/components/ErrorMsg";

const Notifications = () => {
  return (
    <div className="h-full">
      <ErrorMsg message="You have no notifications yet" />
    </div>
  );
};

export default Notifications;
