import ErrorMsg from "@/components/ErrorMsg";

const Messages = () => {
  return (
    <div className="h-full">
      <ErrorMsg message="You have no messages yet" />
    </div>
  );
};

export default Messages;
